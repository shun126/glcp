# CHANGELOG

## 2026-05-14
- Added CI automation to regenerate `glcp/glcp.c` and `glcp/glcp.h` when `gl/glcorearb.h` is updated, and commit `CHANGELOG.md` together.

- Supported OpenGL: **0.0** (placeholder until valid `glcorearb.h` is provided).
